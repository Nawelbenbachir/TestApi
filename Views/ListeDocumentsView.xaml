<UserControl x:Class="TestApi.Views.ListeDocumentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestApi.Views" xmlns:viewmodels="clr-namespace:TestApi.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:DocumentViewModelNoDB}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="Liste des documents" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="{Binding Numero}" Margin="5"/>
                            <TextBlock Grid.Column="1" Text="{Binding Date}" Margin="5"/>
                            <TextBlock Grid.Column="2" Text="{Binding TotalHt}" Margin="5"/>
                            <TextBlock Grid.Column="3" Text="{Binding TotalTva}" Margin="5"/>
                            <TextBlock Grid.Column="4" Text="{Binding TotalTtc}" Margin="5"/>
                            
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </StackPanel>
        <Grid Grid.Column="1" Margin="10">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Détails du document" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Grid.Column="0" Grid.Row="1" Text="Numéro:" FontWeight="Bold"/>
            <TextBox Grid.Column="0" Grid.Row="2" Text="{Binding SelectedDocument.Numero}" Margin="10"/>
            <TextBlock Grid.Column="1" Grid.Row="1" Text="Date:" FontWeight="Bold"/>
            <DatePicker Grid.Column="1" Grid.Row="2" SelectedDate="{Binding SelectedDocument.Date}" Margin="10"/>
            <TextBlock Grid.Column="0" Grid.Row="3" Text="Date Echeance/Validité:" FontWeight="Bold"/>
            <DatePicker Grid.Column="0" Grid.Row="4" SelectedDate="{Binding SelectedDocument.DateEcheanceValidite}" Margin="10"/>
            <TextBlock Grid.Column="1" Grid.Row="3" Text="Statut:" FontWeight="Bold"/> 
            <ComboBox Grid.Column="1" Grid.Row="4" SelectedValue="{Binding SelectedDocument.Statut}" SelectedValuePath="Content" Margin="10">
                <ComboBoxItem Content="En cours"/>
                <ComboBoxItem Content="Payée/Validé"/>
                <ComboBoxItem Content="Annulé"/>
             </ComboBox>
            <ListBox ItemsSource="{Binding Lignes}" SelectedItem="{Binding SelectedLigne}" Grid.ColumnSpan="2" Grid.Row="5">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding CodeProduit}" Margin="5"/>
                            <TextBlock Grid.Column="1" Text="{Binding Description}" Margin="5"/>
                            <TextBlock Grid.Column="2" Text="{Binding Quantite}" Margin="5"/>
                            <TextBlock Grid.Column="3" Text="{Binding TotalHt}" Margin="5"/>
                            <TextBlock Grid.Column="4" Text="{Binding TauxTva}" Margin="5"/>
                            <TextBlock Grid.Column="5" Text="{Binding TotalTtc}" Margin="5"/>

                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
